<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Pressed state -->
    <item android:state_pressed="true">
        <shape>
            <solid android:color="#11000000"/> <!-- Change to your desired pressed color -->
            <stroke android:width="2dp" android:color="@color/light_gray"/> <!-- Border color -->
            <corners android:radius="5dp"/> <!-- Rounded corners -->
        </shape>
    </item>

    <!-- Default state -->
    <item android:top="2dp" android:right="2dp" android:left="2dp" android:bottom="2dp">
        <shape android:shape="rectangle">
            <solid android:color="@color/white" />
            <corners android:radius="5dp" />
        </shape>
    </item>
</selector>
